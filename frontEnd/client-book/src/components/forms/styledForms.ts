import styled from "styled-components";

export const StyledForm = styled.form`
    color: var(--grey-1);
    gap: 10px;
    border: 2px solid var(--primary);
    padding: 20px;
    border-radius: 7px;
`